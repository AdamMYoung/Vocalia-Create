export interface Group {}

export interface Podcast {}

export interface Session {}

export interface User {
  userUID: string;
  userTag: string;
  firstName: string;
  lastName: string;
  pictureUrl: string;
}
